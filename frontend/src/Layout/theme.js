import createTheme from "@mui/material/styles/createTheme";
const theme = createTheme({
    palette: {
        primary: {
        main: "#00669a",
        // contrastText: "#2f9a66",
        },
        secondary: {
            main : '#9a0000'
        }
    },
});
export default theme;